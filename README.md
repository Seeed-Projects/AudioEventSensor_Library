# AudioEventSensor_Library
The `AudioEventSensor` library allows easy integration with an audio event sensor module (like the XU316) over UART, enabling event detection, threshold configuration, and more.
